# cr-check 에 대한 친구G의 조언

**⚠️ 우려되는 점 & 개선 포인트 (Cons & Risks):**

<!-- 1. **타임아웃 리스크 (Critical):**
    - 프론트엔드(`page.tsx`)에서는 5분(300초) 대기를 설정했지만, 배포 환경(Vercel 등)의 Serverless Function은 무료 플랜 기준 보통 **10~60초 내에 강제로 연결을 끊습니다.** 분석이 40~60초 걸린다면, 로컬에서는 잘 되더라도 실제 배포 시 `504 Gateway Timeout`이 발생할 확률이 매우 높습니다. (비동기 큐 도입이나 스트리밍 응답 고려 필요) -->
2. **스크래핑의 한계 (`scraper.py`):**
    - 현재 `requests`와 `BeautifulSoup`을 사용합니다. 이는 네이버/다음 뉴스처럼 정적인 페이지는 잘 긁어오지만, 자바스크립트로 렌더링되는 최신 웹사이트나 봇 차단(Cloudflare 등)이 걸린 사이트는 긁어오지 못할 수 있습니다.
---
