# CR-Check DB 구축 Week 1 — 삼각편대 운영 플레이북

> 작성일: 2026-03-28  
> 목적: Gamnamu가 중계자로서 각 시점에 누구에게, 어떤 프롬프트를 전달해야 하는지 빠짐없이 정리  
> 역할: Claude Code CLI(코딩) → Claude.ai(1차 감리) → Antigravity/Gemini(2차 더블체크) → Gamnamu(승인)

---

## 공통 원칙

- **Claude Code CLI 세션 시작 시 반드시**: `/effort high` 설정 → Plan Mode로 문서 숙지 후 Normal Mode 전환
- **컨텍스트 관리**: 50%에서 `/compact` 선제 실행. 변경 전 `/diff`로 사전 확인
- **쓰기 작업은 반드시 Migration 파일로만**. MCP는 읽기 전용 조회 전용
- **KJA 접두어 절대 사용 금지** → JCE가 올바른 접두어

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## M1: 전체 스키마 생성 (Day 1)
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### STEP 1. Claude Code CLI — 세션 시작 및 작업 지시

**[시점]** Day 1 시작. cr-check 프로젝트 디렉토리에서 Claude Code CLI 실행.

**[프롬프트 — Claude Code CLI에게]**

```
/effort high

지금부터 CR-Check Hybrid RAG DB의 Week 1 M1(전체 스키마 생성) 작업을 시작한다.

■ 사전 숙지 (Plan Mode로 먼저 읽을 것)
1. docs/DB_AND_RAG_MASTER_PLAN_v4.0.md — 섹션 6.1(테이블 정의 SQL)이 핵심
2. docs/SESSION_CONTEXT_2026-03-25_v12.md — "다음 세션의 Claude에게" 섹션의 10가지 핵심 지침
3. CLAUDE.md — 프로젝트 루트의 작업 규칙

■ 작업 내용
supabase/ 폴더가 아직 없으므로, 필요하면 supabase init을 먼저 실행하라.
마스터 플랜 v4.0 섹션 6.1의 SQL 정의를 기반으로 supabase/migrations/ 폴더에 Migration SQL 파일을 생성하라.

■ 세부 요구사항
- 파일명 형식: YYYYMMDDHHMMSS_create_cr_check_schema.sql (Supabase CLI 규칙)
- 테이블 생성 순서: FK 의존성을 고려한 논리적 순서 (부모 테이블 먼저)
- 반드시 포함할 것:
  · pgvector 확장 활성화 (CREATE EXTENSION IF NOT EXISTS vector)
  · ethics_codes 테이블에 is_citable BOOLEAN NOT NULL DEFAULT TRUE 컬럼
  · ivfflat 인덱스 없음 (262개 규모에서 sequential scan이 최적)
  · get_ethics_for_patterns() RPC 함수 — 재귀 CTE로 parent chain 수집, is_citable 필터 적용
  · 모든 접두어는 JCE (KJA 아님)
- RLS(Row Level Security) 정책은 이 단계에서 포함하지 않음 (M5에서 처리)

■ 작업 완료 후
- 생성된 Migration 파일의 전체 내용을 출력하라
- 테이블 간 FK 관계를 요약 정리하라
- 마스터 플랜 섹션 6.1과 대조하여 누락된 항목이 없는지 자체 점검 결과를 보고하라

코드를 작성하기 전에 먼저 Plan Mode에서 위 문서들을 읽고, 작업 계획을 보여줘라.
```

**[체크포인트]** Claude Code CLI가 Plan을 제시하면 검토 후 "진행해" 승인. 이상 있으면 수정 지시.

---

### STEP 2. Claude Code CLI — Migration 파일 생성 완료 확인

**[시점]** Claude Code CLI가 Migration SQL 파일 생성을 완료하고 결과를 출력한 직후.

**[Gamnamu 액션]**
1. Claude Code CLI가 출력한 SQL 전문과 자체 점검 결과를 복사
2. 생성된 파일 경로 확인: `supabase/migrations/YYYYMMDDHHMMSS_create_cr_check_schema.sql`
3. 아래 STEP 3으로 이동하여 Claude.ai에게 1차 감리 요청

---

### STEP 3. Claude.ai — 1차 감리 요청

**[시점]** STEP 2 직후. Claude.ai 채팅으로 이동.

**[프롬프트 — Claude.ai에게]**

```
CR-Check DB 구축 M1 감리를 요청한다.

Claude Code CLI가 마스터 플랜 v4.0 섹션 6.1 기반으로 생성한 Migration SQL 파일이다.
아래 SQL을 감리해줘.

---
[여기에 Migration SQL 전문 붙여넣기]
---

■ 감리 체크리스트 (모든 항목에 대해 ✅/❌ 판정)

[A] SQL 정합성
- [ ] CREATE EXTENSION IF NOT EXISTS vector; 포함 여부
- [ ] 테이블 생성 순서가 FK 의존성을 올바르게 반영하는가
- [ ] 모든 FK 제약조건이 올바른 테이블·컬럼을 참조하는가
- [ ] 데이터 타입이 마스터 플랜과 일치하는가 (특히 embedding vector(1536))
- [ ] NOT NULL, DEFAULT 값이 마스터 플랜과 일치하는가

[B] 앙상블 반영 4건 확인
- [ ] ivfflat 인덱스가 없는가 (있으면 ❌)
- [ ] ethics_codes에 is_citable BOOLEAN NOT NULL DEFAULT TRUE가 있는가
- [ ] get_ethics_for_patterns() 함수가 재귀 CTE로 parent chain을 수집하는가
- [ ] get_ethics_for_patterns() 내 WHERE 절에 is_citable = TRUE 필터가 있는가
- [ ] 모든 접두어가 JCE인가 (KJA가 하나라도 있으면 ❌)

[C] 컬럼 누락 점검
- [ ] 마스터 플랜 섹션 6.1의 모든 테이블이 빠짐없이 존재하는가
- [ ] 각 테이블의 모든 컬럼이 빠짐없이 존재하는가
- [ ] junction table(pattern_ethics_map 등)의 relation_type, strength 컬럼 존재 여부

[D] 구조적 우려사항
- [ ] 순환 참조(circular reference) 가능성은 없는가
- [ ] parent_code_id 자기참조 FK가 올바르게 설정되었는가
- [ ] 향후 M2(시드 데이터)에서 2-패스 삽입이 가능한 구조인가

각 항목에 대해 판정 결과와 근거를 설명하고, 문제가 있으면 수정 제안을 구체적으로 제시해줘.
마지막에 종합 판정(승인/조건부 승인/반려)과 이유를 명시해줘.
```

**[체크포인트]** Claude.ai의 감리 결과를 읽고 이해. 특히 ❌ 판정 항목이 있는지 확인.

---

### STEP 4. Antigravity — 2차 독립 감리 요청

**[시점]** STEP 3의 Claude.ai 감리가 완료된 직후. Antigravity(Gemini) 채팅으로 이동.

**[프롬프트 — Antigravity에게]**

```
CR-Check DB 구축 M1의 2차 독립 감리를 요청한다.

Claude Code CLI가 생성한 Migration SQL 파일에 대해, Claude.ai가 1차 감리를 완료했다.
너는 비-Anthropic 계열의 독립적 관점에서 같은 파일을 리뷰해줘.

■ Migration SQL 전문:
---

-- ============================================
-- CR-Check Hybrid RAG DB Schema
-- Migration: 20260328000000_create_cr_check_schema
--
-- 기반 문서: DB_AND_RAG_MASTER_PLAN_v4.0.md 섹션 6.1
--            DB_BUILD_EXECUTION_GUIDE.md 섹션 2-2
-- 앙상블 검증 반영 (2026-03-25):
--   - ivfflat 인덱스 제거 (5/5 합의)
--   - is_citable 컬럼 추가 (5/5 합의)
--   - 재귀 CTE 롤업 (get_ethics_for_patterns)
--   - KJA → JCE 접두어 수정
-- ============================================

-- ============================================
-- 0. Extension
-- ============================================
CREATE EXTENSION IF NOT EXISTS vector;

-- ============================================
-- 1. 문제적 보도관행 패턴 (Phase 0)
-- ============================================
CREATE TABLE public.patterns (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  code TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  description TEXT NOT NULL,
  category TEXT,
  subcategory TEXT,
  is_meta_pattern BOOLEAN DEFAULT FALSE,
  hierarchy_level INT,          -- 1=대분류, 2=중분류, 3=소분류
  parent_pattern_id BIGINT REFERENCES public.patterns(id),
  locale TEXT NOT NULL DEFAULT 'ko-KR',
  description_embedding vector(1536),
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_patterns_code ON public.patterns(code);
CREATE INDEX idx_patterns_locale ON public.patterns(locale);
-- 벡터 인덱스 불필요: ~102개 패턴 규모에서 sequential scan이
-- 100% recall + 1ms 미만 응답으로 ivfflat/HNSW보다 우수.
-- 1만 건 이상 확장 시 HNSW 도입 검토. (앙상블 검증 2026-03-25, 5/5 합의)

-- ============================================
-- 2. 언론윤리규범 조항 (Phase 0, 버전 관리 포함)
-- ============================================
CREATE TABLE public.ethics_codes (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  code TEXT NOT NULL,
  title TEXT NOT NULL,
  full_text TEXT NOT NULL,
  source TEXT NOT NULL,
  article_number TEXT,
  tier INT NOT NULL CHECK (tier BETWEEN 1 AND 4),
  tier_rationale TEXT,
  parent_code_id BIGINT REFERENCES public.ethics_codes(id),
  domain TEXT DEFAULT 'general',
  locale TEXT NOT NULL DEFAULT 'ko-KR',
  -- 버전 관리
  version INT NOT NULL DEFAULT 1,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  effective_from DATE NOT NULL DEFAULT CURRENT_DATE,
  effective_until DATE,
  superseded_by BIGINT REFERENCES public.ethics_codes(id),
  change_reason TEXT,
  -- 인용 적합성 필터 (앙상블 검증 5/5 합의)
  is_citable BOOLEAN NOT NULL DEFAULT TRUE,
  text_embedding vector(1536),
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  CONSTRAINT unique_ethics_code_version UNIQUE (code, version)
);

CREATE INDEX idx_ethics_code ON public.ethics_codes(code);
CREATE INDEX idx_ethics_locale ON public.ethics_codes(locale);
CREATE INDEX idx_ethics_active ON public.ethics_codes(is_active);
-- 벡터 인덱스 불필요: ~160개 규범 규모에서 sequential scan 충분.

-- ============================================
-- 3. 규범 위계 관계 — 다대다 대비 Junction Table (Phase 0)
-- ============================================
CREATE TABLE public.ethics_code_hierarchy (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  parent_code_id BIGINT REFERENCES public.ethics_codes(id) NOT NULL,
  child_code_id BIGINT REFERENCES public.ethics_codes(id) NOT NULL,
  relation_note TEXT,
  CONSTRAINT unique_hierarchy UNIQUE (parent_code_id, child_code_id)
);

-- ============================================
-- 4. 패턴 ↔ 윤리규범 관계 (Phase 0)
-- ============================================
CREATE TABLE public.pattern_ethics_relations (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  pattern_id BIGINT REFERENCES public.patterns(id) ON DELETE CASCADE NOT NULL,
  ethics_code_id BIGINT REFERENCES public.ethics_codes(id) ON DELETE CASCADE NOT NULL,
  relation_type TEXT NOT NULL CHECK (relation_type IN (
    'violates', 'related_to', 'exception_of'
  )),
  strength TEXT DEFAULT 'moderate' CHECK (strength IN ('strong', 'moderate', 'weak')),
  reasoning TEXT,
  examples TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  CONSTRAINT unique_pattern_ethics UNIQUE (pattern_id, ethics_code_id, relation_type)
);

CREATE INDEX idx_rel_pattern ON public.pattern_ethics_relations(pattern_id);
CREATE INDEX idx_rel_ethics ON public.pattern_ethics_relations(ethics_code_id);

-- ============================================
-- 5. 패턴 간 관계 (Phase 0)
-- ============================================
CREATE TABLE public.pattern_relations (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  source_pattern_id BIGINT REFERENCES public.patterns(id) NOT NULL,
  target_pattern_id BIGINT REFERENCES public.patterns(id) NOT NULL,
  relation_type TEXT NOT NULL CHECK (relation_type IN (
    'co_occurs', 'escalates_to', 'variant_of', 'inferred_by'
  )),
  description TEXT,
  source TEXT DEFAULT 'manual' CHECK (source IN (
    'manual', 'embedding_similarity', 'llm_extracted', 'co_occurrence_mined'
  )),
  confidence FLOAT DEFAULT 1.0,
  verified BOOLEAN DEFAULT FALSE,
  CONSTRAINT unique_pattern_rel UNIQUE (source_pattern_id, target_pattern_id, relation_type)
);

-- ============================================
-- 6. Articles — 기사 메타데이터 (Phase 1, Auth 제거)
-- ============================================
CREATE TABLE public.articles (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  url TEXT NOT NULL,
  title TEXT NOT NULL,
  publisher TEXT,
  journalist TEXT,
  publish_date TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL,
  CONSTRAINT articles_url_key UNIQUE (url)
);

CREATE INDEX idx_articles_url ON public.articles(url);
CREATE INDEX idx_articles_publisher ON public.articles(publisher);
CREATE INDEX idx_articles_created_at ON public.articles(created_at DESC);

COMMENT ON TABLE public.articles IS '기사 원본 메타데이터 (중복 분석 방지용)';

-- ============================================
-- 7. Analysis Results — AI 분석 결과 (Phase 1, Auth 제거)
-- ============================================
CREATE TABLE public.analysis_results (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  article_id BIGINT REFERENCES public.articles(id) ON DELETE CASCADE NOT NULL,
  comprehensive_report TEXT,
  journalist_report TEXT,
  student_report TEXT,
  phase1_model TEXT DEFAULT 'claude-haiku-4-5-20251001',
  phase2_model TEXT DEFAULT 'claude-sonnet-4-6',
  duration_seconds FLOAT,
  detected_categories JSONB,
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL
);

CREATE INDEX idx_analysis_article ON public.analysis_results(article_id);
CREATE INDEX idx_analysis_created_at ON public.analysis_results(created_at DESC);

COMMENT ON TABLE public.analysis_results IS 'AI 분석 결과 저장 (익명, 공개 URL 공유)';
COMMENT ON COLUMN public.analysis_results.detected_categories IS 'Phase 1에서 탐지된 문제 카테고리 ID 목록';

-- ============================================
-- 8. Feedbacks — 익명 사용자 피드백 (Phase 3 활성화)
-- ============================================
CREATE TABLE public.feedbacks (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  analysis_id BIGINT REFERENCES public.analysis_results(id) ON DELETE CASCADE NOT NULL,
  rating INT CHECK (rating BETWEEN 1 AND 5),
  comment TEXT,
  created_at TIMESTAMPTZ DEFAULT now() NOT NULL
);

CREATE INDEX idx_feedbacks_analysis ON public.feedbacks(analysis_id);

COMMENT ON TABLE public.feedbacks IS '익명 사용자 피드백 (user_id 없이 analysis_id + rating + comment만 저장)';

-- ============================================
-- 9. 분석 리포트 규범 스냅샷 — 시점 보존 (Phase 0)
-- ============================================
CREATE TABLE public.analysis_ethics_snapshot (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  analysis_id BIGINT REFERENCES public.analysis_results(id) ON DELETE CASCADE NOT NULL,
  ethics_code_id BIGINT REFERENCES public.ethics_codes(id) NOT NULL,
  snapshot_full_text TEXT NOT NULL,
  snapshot_version INT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);

COMMENT ON TABLE public.analysis_ethics_snapshot IS
  '분석 리포트가 참조한 규범 조항의 시점 스냅샷.
   규범이 개정되어도 과거 리포트의 인용 근거가 보존됨.
   저장 범위: Sonnet이 실제로 <cite ref/>로 인용한 규범만 스냅샷.';

-- ============================================
-- 10. 뷰: 활성 규범만 검색
-- ============================================
CREATE VIEW public.active_ethics_codes AS
  SELECT * FROM public.ethics_codes WHERE is_active = TRUE;

-- ============================================
-- 11. 뷰: 규범 변경 이력 추적
-- ============================================
CREATE VIEW public.ethics_codes_history AS
  SELECT ec.code, ec.version, ec.title,
         ec.effective_from, ec.effective_until,
         ec.is_active, ec.change_reason,
         successor.code AS successor_code,
         successor.version AS successor_version
  FROM public.ethics_codes ec
  LEFT JOIN public.ethics_codes successor ON ec.superseded_by = successor.id
  ORDER BY ec.code, ec.version;

-- ============================================
-- 12. RPC 함수: 패턴 후보 검색 (Haiku 단계용)
-- ============================================
CREATE OR REPLACE FUNCTION search_pattern_candidates(
  query_embedding vector(1536),
  match_threshold FLOAT DEFAULT 0.5,
  match_count INT DEFAULT 10
)
RETURNS TABLE (
  pattern_id BIGINT,
  pattern_code TEXT,
  pattern_name TEXT,
  pattern_description TEXT,
  similarity FLOAT
) AS $$
BEGIN
  RETURN QUERY
  SELECT p.id, p.code, p.name, p.description,
         1 - (p.description_embedding <=> query_embedding) AS similarity
  FROM public.patterns p
  WHERE p.is_meta_pattern = FALSE
    AND 1 - (p.description_embedding <=> query_embedding) > match_threshold
  ORDER BY similarity DESC
  LIMIT match_count;
END;
$$ LANGUAGE plpgsql STABLE;

-- ============================================
-- 13. RPC 함수: 확정 패턴에 대한 규범 정밀 조회 + 롤업 parent chain
--     (Sonnet 단계용, 앙상블 검증 반영: 재귀 CTE)
-- ============================================
CREATE OR REPLACE FUNCTION get_ethics_for_patterns(
  confirmed_pattern_ids BIGINT[]
)
RETURNS TABLE (
  pattern_id BIGINT,
  pattern_code TEXT,
  ethics_code_id BIGINT,
  ethics_code TEXT,
  ethics_title TEXT,
  ethics_full_text TEXT,
  ethics_tier INT,
  relation_type TEXT,
  strength TEXT,
  reasoning TEXT
) AS $$
BEGIN
  RETURN QUERY
  WITH RECURSIVE direct_codes AS (
    -- 1단계: pattern_ethics_relations에서 직접 연결된 규범
    SELECT per.pattern_id, p.code AS p_code,
           ec.id AS ec_id, ec.code AS ec_code, ec.title, ec.full_text, ec.tier,
           per.relation_type, per.strength, per.reasoning
    FROM public.pattern_ethics_relations per
    JOIN public.patterns p ON per.pattern_id = p.id
    JOIN public.active_ethics_codes ec ON per.ethics_code_id = ec.id
    WHERE per.pattern_id = ANY(confirmed_pattern_ids)
      AND ec.is_citable = TRUE
  ),
  parent_chain AS (
    -- 2단계: 직접 규범의 parent chain을 재귀적으로 수집 (구체→포괄 롤업)
    SELECT ec.id, ec.code, ec.title, ec.full_text, ec.tier, ec.parent_code_id
    FROM public.ethics_codes ec
    WHERE ec.id IN (SELECT ec_id FROM direct_codes)
      AND ec.is_active = TRUE
    UNION
    SELECT parent.id, parent.code, parent.title, parent.full_text, parent.tier, parent.parent_code_id
    FROM public.ethics_codes parent
    JOIN parent_chain child ON parent.id = child.parent_code_id
    WHERE parent.is_active = TRUE
      AND parent.is_citable = TRUE
  )
  -- 직접 관계 규범 반환
  SELECT dc.pattern_id, dc.p_code,
         dc.ec_id, dc.ec_code, dc.title, dc.full_text, dc.tier,
         dc.relation_type, dc.strength, dc.reasoning
  FROM direct_codes dc
  UNION
  -- 롤업 상위 규범 반환 (직접 관계에 없는 parent만)
  SELECT DISTINCT d.pattern_id, d.p_code,
         pc.id, pc.code, pc.title, pc.full_text, pc.tier,
         'related_to'::TEXT, 'moderate'::TEXT, 'parent chain rollup'::TEXT
  FROM parent_chain pc
  CROSS JOIN (SELECT DISTINCT dc2.pattern_id, dc2.p_code FROM direct_codes dc2) d
  WHERE pc.id NOT IN (SELECT dc3.ec_id FROM direct_codes dc3)
  ORDER BY tier;
END;
$$ LANGUAGE plpgsql STABLE;

-- ============================================
-- 14. Phase 2 통계 함수: 실시간 트렌드
-- ============================================
CREATE OR REPLACE FUNCTION get_trending_articles(hours_ago INT DEFAULT 24)
RETURNS TABLE (
  article_id BIGINT,
  title TEXT,
  url TEXT,
  publisher TEXT,
  analysis_count BIGINT,
  latest_analysis TIMESTAMPTZ
) AS $$
BEGIN
  RETURN QUERY
  SELECT a.id, a.title, a.url, a.publisher,
         COUNT(ar.id) AS analysis_count,
         MAX(ar.created_at) AS latest_analysis
  FROM public.articles a
  JOIN public.analysis_results ar ON a.id = ar.article_id
  WHERE ar.created_at >= NOW() - INTERVAL '1 hour' * hours_ago
  GROUP BY a.id, a.title, a.url, a.publisher
  ORDER BY analysis_count DESC, latest_analysis DESC
  LIMIT 10;
END;
$$ LANGUAGE plpgsql STABLE;

-- ============================================
-- 15. Phase 2 통계 함수: 언론사별 통계
-- ============================================
CREATE OR REPLACE FUNCTION get_publisher_stats()
RETURNS TABLE (
  publisher TEXT,
  total_analyses BIGINT,
  avg_duration FLOAT
) AS $$
BEGIN
  RETURN QUERY
  SELECT a.publisher, COUNT(ar.id), AVG(ar.duration_seconds)
  FROM public.articles a
  JOIN public.analysis_results ar ON a.id = ar.article_id
  WHERE a.publisher IS NOT NULL
  GROUP BY a.publisher
  ORDER BY total_analyses DESC
  LIMIT 20;
END;
$$ LANGUAGE plpgsql STABLE;

-- ============================================
-- 16. Phase 2 통계 함수: 전체 통계 요약 (Auth 제거)
-- ============================================
CREATE OR REPLACE FUNCTION get_overall_stats()
RETURNS TABLE (
  total_articles BIGINT,
  total_analyses BIGINT,
  avg_duration FLOAT
) AS $$
BEGIN
  RETURN QUERY
  SELECT COUNT(DISTINCT a.id),
         COUNT(ar.id),
         AVG(ar.duration_seconds)
  FROM public.articles a
  LEFT JOIN public.analysis_results ar ON a.id = ar.article_id;
END;
$$ LANGUAGE plpgsql STABLE;
---

■ 참조 문서: 마스터 플랜 v4.0 섹션 6.1 (필요하면 프로젝트 내 docs/DB_AND_RAG_MASTER_PLAN_v4.0.md를 직접 읽어라)

■ 감리 체크리스트 (.gemini/GEMINI.md의 체크리스트도 참조)

1. SQL 문법 및 PostgreSQL 호환성에 문제가 없는가?
2. 테이블 생성 순서가 FK 의존성을 올바르게 반영하는가?
3. 마스터 플랜 v4.0과 대조하여 누락된 테이블/컬럼이 있는가?
4. ethics_codes 테이블에 is_citable 컬럼이 올바르게 정의되었는가?
5. ivfflat 인덱스가 포함되어 있지 않은가? (있으면 지적)
6. get_ethics_for_patterns() 함수의 재귀 CTE 로직이 올바른가?
7. parent_code_id 자기참조 FK 구조가 2-패스 삽입에 적합한가?
8. Claude.ai 1차 감리에서 놓쳤을 수 있는 구조적 문제가 있는가?
9. 참고: 1차 감리에서 get_ethics_for_patterns() 함수의 WITH RECURSIVE 누락이 발견되어 수정 반영된 상태다. 270행에 올바르게 반영되었는지도 확인해줘.

종합 판정(승인/조건부 승인/반려)과 함께, Claude.ai 감리와 다른 관점의 발견 사항이 있으면 별도 표기해줘.
```

**[체크포인트]** 두 감리 결과를 나란히 비교. 불일치 항목이 있으면 Gamnamu가 판단.

---

### STEP 5. Gamnamu — 감리 결과 비교 및 최종 판단

**[시점]** Claude.ai + Antigravity 양쪽 감리가 모두 완료된 후.

**[판단 기준]**
- 양쪽 모두 "승인" → STEP 6으로 진행
- 한쪽이라도 "반려" → 수정 사항을 정리하여 STEP 5-1로
- "조건부 승인" → 조건 항목을 검토하여 수정 필요 여부 판단

### STEP 5-1. (조건부) Claude Code CLI — 수정 지시

**[시점]** 감리에서 수정 사항이 발견된 경우에만.

**[프롬프트 — Claude Code CLI에게]**

```
M1 Migration 파일에 대해 1차·2차 감리 결과 수정이 필요한 항목이 발견되었다.

■ 수정 사항:
[감리에서 지적된 구체적 항목들을 나열]

위 항목들을 Migration 파일에 반영하고, 수정 전후 diff를 보여줘.
수정 완료 후 자체 점검을 다시 수행하라.
```

**[체크포인트]** 수정된 파일을 다시 STEP 3~4 감리 루프로 보낼지, 수정이 경미하여 바로 STEP 6으로 진행할지 Gamnamu가 판단.

---

### STEP 6. Claude Code CLI — 로컬 DB 테스트

**[시점]** 감리 승인 완료 후.

**[프롬프트 — Claude Code CLI에게]**

```
M1 감리가 승인되었다. 로컬 Docker DB에서 Migration을 테스트하라.

1. supabase start 실행 (Docker가 실행 중인지 먼저 확인)
2. Migration 자동 적용 확인
3. 적용 후 다음을 검증:
   - 모든 테이블이 생성되었는지 \dt 로 확인
   - ethics_codes 테이블 구조 확인 (\d ethics_codes) — is_citable 컬럼 존재 확인
   - FK 제약조건 확인 (\d+ pattern_ethics_map)
   - get_ethics_for_patterns() 함수 존재 확인 (\df get_ethics_for_patterns)
   - pgvector 확장 확인 (\dx)
4. 오류가 있으면 오류 메시지 전문을 보고하라
5. 성공하면 테이블 목록과 각 테이블의 컬럼 수를 요약 보고하라
```

**[체크포인트]** 오류 발생 시 → STEP 5-1로 돌아가 수정. 성공 시 → STEP 7로.

---

### STEP 7. Antigravity (MCP) — 테이블 구조 읽기 전용 확인

**[시점]** STEP 6의 로컬 테스트 성공 직후.

**[프롬프트 — Antigravity에게]**

```
M1 Migration이 로컬 DB에 적용되었다.
MCP(읽기 전용)를 사용하여 다음을 확인해줘:

1. list_tables로 생성된 테이블 목록 조회
2. 각 테이블의 컬럼 구조가 마스터 플랜 v4.0 섹션 6.1과 일치하는지 확인
3. ethics_codes 테이블에 is_citable 컬럼이 존재하는지 확인
4. get_ethics_for_patterns() 함수가 존재하는지 확인

결과를 보고해줘.
```

**[체크포인트]** Antigravity 보고에 이상이 없는지 확인.

---

### STEP 8. Gamnamu — Supabase Studio 시각적 확인 및 배포 승인

**[시점]** STEP 7 완료 후.

**[Gamnamu 직접 수행]**
1. 브라우저에서 `http://localhost:54323` 접속 (Supabase Studio)
2. Table Editor에서 모든 테이블이 보이는지 시각 확인
3. 각 테이블을 클릭하여 컬럼 구조 훑어보기
4. 특히 ethics_codes의 is_citable, parent_code_id 확인
5. 이상 없으면 배포 승인 결정

---

### STEP 9. Claude Code CLI — 클라우드 배포

**[시점]** Gamnamu의 배포 승인 직후.

**[프롬프트 — Claude Code CLI에게]**

```
M1 배포를 승인한다. 클라우드에 배포하라.

1. supabase link --project-ref [프로젝트 ref] (이미 링크되어 있으면 스킵)
2. supabase db push 실행
3. 배포 결과를 보고하라 — 성공/실패, 적용된 Migration 파일명
4. 배포 후 원격 DB에서도 테이블 목록을 확인하라

⚠️ 이 명령은 Antigravity Deny List에 포함되어 있으므로, 
   Claude Code CLI에서만 실행한다. Antigravity에서는 실행하지 않는다.
```

**[체크포인트]** 배포 성공 확인 → M1 완료. Day 1 종료.

---

### STEP 10. (Day 1 마무리) 세션 컨텍스트 갱신

**[프롬프트 — Claude Code CLI에게]**

```
M1이 완료되었다. 다음을 수행하라:

1. docs/SESSION_CONTEXT_2026-03-25_v12.md를 복사하여 
   docs/SESSION_CONTEXT_2026-03-28_v13.md를 생성하라
2. v13에 다음 내용을 반영:
   - M1 완료 상태 기록 (날짜, 생성된 Migration 파일명, 테이블 수)
   - 감리 과정에서 발견/수정된 사항 요약
   - "다음 세션에서 할 일"을 M2(시드 데이터 입력)로 갱신
3. v12는 _archive_superseded/로 이동
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## M2: 시드 데이터 입력 (Day 2)
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### STEP 11. Claude Code CLI — 시드 스크립트 생성

**[시점]** Day 2 시작. Claude Code CLI 세션 시작.

**[프롬프트 — Claude Code CLI에게]**

```
/effort high

M1(스키마)이 완료된 상태다. 지금부터 M2(시드 데이터 입력) 작업을 시작한다.

■ 사전 숙지 (Plan Mode로 먼저 읽을 것)
1. docs/DB_AND_RAG_MASTER_PLAN_v4.0.md — 섹션 7(시드 데이터) 및 섹션 6.1(테이블 정의)
2. docs/DB_BUILD_EXECUTION_GUIDE.md — 2-패스 삽입 패턴 섹션
3. docs/SESSION_CONTEXT (최신 버전)
4. CLAUDE.md

■ 입력 데이터 파일
- docs/ethics_codes_mapping.json — 윤리 규범 394개
- docs/current-criteria_v2_active.md — 분석 패턴 119개

■ 작업 내용
시드 데이터 입력 스크립트를 supabase/migrations/ 폴더에 Migration SQL 파일로 생성하라.

■ 핵심 요구사항

1. **2-패스 삽입 패턴 (필수)**:
   - 1차 패스: 모든 레코드를 parent_code_id = NULL로 삽입
   - 2차 패스: code 문자열("JEC-8")을 id(BIGINT)로 매핑하여 parent_code_id UPDATE
   - 이유: JSON의 parent_code_id가 문자열이지만 DB는 BIGINT FK

2. **is_citable 필드 처리**:
   - ethics_codes_mapping.json에 is_citable 필드가 있으면 그대로 사용
   - 없으면 기본값 TRUE, 단 서문/부칙 성격의 항목은 FALSE로 설정
   - 서문/부칙 해당 항목 목록을 명시적으로 보여줘라

3. **relation_type 매핑**:
   - 레이블링 v3의 5가지 유형(직접/보조/유추/상위/최상위)을
   - DB의 3가지(violates/related_to/exception_of) + strength로 매핑
   - 매핑 규칙을 먼저 제시하고 승인받은 후 적용

4. **접두어**: 모든 곳에서 JCE 사용. KJA 절대 금지.

■ 작업 완료 후
- 생성된 시드 Migration 파일의 전체 내용 출력
- 삽입 대상 요약: 각 테이블별 레코드 수
- 2-패스 삽입의 1차/2차 패스 로직 설명
- is_citable = FALSE로 설정한 항목 목록
- 자체 점검: ethics_codes_mapping.json의 394개와 삽입 레코드 수 일치 여부

코드 작성 전에 Plan Mode에서 문서를 읽고, relation_type 매핑 규칙을 먼저 제시하라.
```

**[체크포인트]** 
- Plan 단계에서 relation_type 매핑 규칙이 제시되면 검토 후 승인/수정
- is_citable = FALSE 대상 목록이 제시되면 검토 후 승인/수정
- 두 가지 승인 후 "진행해" 지시

---

### STEP 12. Claude.ai — 시드 스크립트 1차 감리

**[시점]** Claude Code CLI가 시드 Migration 파일 생성을 완료한 직후.

**[프롬프트 — Claude.ai에게]**

```
CR-Check DB 구축 M2(시드 데이터 입력) 감리를 요청한다.

Claude Code CLI가 생성한 시드 Migration SQL 파일이다.

---
[여기에 시드 Migration SQL 전문 붙여넣기]
---

■ 감리 체크리스트

[A] 2-패스 삽입 정합성
- [ ] 1차 패스에서 모든 레코드가 parent_code_id = NULL로 삽입되는가
- [ ] 2차 패스에서 code→id 매핑 UPDATE가 올바른가
- [ ] 2차 패스의 WHERE 절이 정확한 code 문자열을 참조하는가
- [ ] 존재하지 않는 parent_code를 참조하는 레코드는 없는가

[B] 데이터 정확성
- [ ] ethics_codes 테이블 삽입 레코드 수가 394개와 일치하는가
- [ ] 패턴(patterns) 테이블 삽입 레코드 수가 119개와 일치하는가
- [ ] tier 분류(tier 1~4)가 올바른가 — 대분류별로 샘플 체크
- [ ] 접두어가 모두 JCE인가 (KJA가 하나라도 있으면 ❌)

[C] is_citable 처리
- [ ] is_citable = FALSE로 설정된 항목이 서문/부칙 성격에 해당하는가
- [ ] 인용 가능해야 할 항목이 FALSE로 잘못 설정되지 않았는가

[D] relation_type + strength 매핑
- [ ] 매핑 규칙이 레이블링 v3의 의도를 올바르게 반영하는가
- [ ] pattern_ethics_map junction table의 데이터가 정확한가

[E] 구조적 안전성
- [ ] 시드 스크립트가 idempotent한가 (재실행 시 중복 삽입 방지)
- [ ] 트랜잭션으로 감싸져 있는가 (중간 실패 시 롤백)

각 항목에 대해 ✅/❌ 판정과 근거를 설명해줘.
특히 [B]의 데이터 정확성은 ethics_codes_mapping.json과 직접 대조하여 
샘플 5~10개를 무작위로 뽑아 검증해줘.

종합 판정(승인/조건부 승인/반려)과 이유를 명시해줘.
```

---

### STEP 13. Antigravity — 시드 스크립트 2차 감리

**[시점]** Claude.ai 감리 완료 직후. Antigravity 채팅으로 이동.

**[프롬프트 — Antigravity에게]**

```
CR-Check DB 구축 M2(시드 데이터 입력)의 2차 독립 감리를 요청한다.

■ 시드 Migration SQL 전문:
---
[여기에 시드 Migration SQL 전문 붙여넣기]
---

■ 참조: docs/ethics_codes_mapping.json (394개 규범), docs/current-criteria_v2_active.md (119개 패턴)

■ 감리 포인트 (Claude.ai와 다른 관점에서)

1. 2-패스 삽입 로직의 SQL 문법이 올바른가?
2. 394개 규범 데이터의 무결성 — JSON 원본과 SQL INSERT 값을 무작위 10건 대조
3. 119개 패턴 데이터의 무결성 — 원본과 SQL INSERT 값을 무작위 5건 대조
4. parent_code_id 매핑에서 순환 참조가 발생하지 않는가?
5. is_citable = FALSE 항목들이 적절한가?
6. relation_type 매핑이 논리적으로 타당한가?
7. Claude.ai 1차 감리에서 놓쳤을 수 있는 데이터 오류가 있는가?

■ 추가 요청
감리 완료 후, MCP(읽기 전용)를 사용하여 로컬 DB에 시드 데이터가 
정상 입력되었는지 확인해줘:
- SELECT count(*) FROM ethics_codes;
- SELECT count(*) FROM patterns;
- SELECT count(*) FROM pattern_ethics_map;
- SELECT * FROM ethics_codes WHERE is_citable = FALSE;

종합 판정(승인/조건부 승인/반려)과 함께 보고해줘.
```

---

### STEP 14. Gamnamu — tier 분류 최종 확인

**[시점]** 양쪽 감리 완료 후.

**[Gamnamu 직접 수행]**
1. 두 감리 결과 비교 → 불일치 항목 판단
2. **tier 분류 직접 확인** (이것만은 Gamnamu가 직접 해야 함):
   - is_citable = FALSE 목록이 적절한지
   - tier 1~4 분류가 CR-Check의 의도에 부합하는지
3. 수정 필요 시 → STEP 14-1로
4. 승인 시 → STEP 15로

### STEP 14-1. (조건부) Claude Code CLI — 시드 데이터 수정

**[프롬프트 — Claude Code CLI에게]**

```
M2 시드 데이터에 대해 감리 결과 수정이 필요하다.

■ 수정 사항:
[구체적 수정 항목 나열]

시드 Migration 파일을 수정하고, 수정 전후 diff를 보여줘.
수정 후 자체 점검을 다시 수행하라.
```

---

### STEP 15. Claude Code CLI — 시드 데이터 로컬 적용 및 검증

**[시점]** 감리 승인 완료 후.

**[프롬프트 — Claude Code CLI에게]**

```
M2 시드 데이터 감리가 승인되었다. 로컬 DB에 적용하고 검증하라.

1. supabase db reset (또는 새 Migration 적용)으로 시드 데이터 입력
2. 적용 후 검증:
   - SELECT count(*) FROM ethics_codes; → 394 확인
   - SELECT count(*) FROM patterns; → 119 확인  
   - SELECT count(*) FROM pattern_ethics_map; → 예상 건수 보고
   - SELECT * FROM ethics_codes WHERE is_citable = FALSE; → 목록 출력
   - SELECT * FROM ethics_codes WHERE parent_code_id IS NOT NULL LIMIT 10; → 부모 매핑 샘플
   - 2-패스 삽입이 정상 작동했는지: parent_code_id가 NULL이 아닌 레코드 수
3. get_ethics_for_patterns() 함수 테스트:
   - 임의의 pattern_id로 호출하여 관련 규범이 반환되는지 확인
   - 재귀 CTE가 parent chain을 따라 상위 규범도 반환하는지 확인
   - is_citable = FALSE인 규범이 결과에서 제외되는지 확인
4. 검증 결과 전체를 보고하라
```

---

### STEP 16. Antigravity (MCP) — 시드 데이터 최종 확인

**[시점]** STEP 15 검증 성공 후.

**[프롬프트 — Antigravity에게]**

```
M2 시드 데이터가 로컬 DB에 적용되었다.
MCP(읽기 전용)로 다음을 최종 확인해줘:

1. ethics_codes 테이블: 총 건수, is_citable = FALSE 건수
2. patterns 테이블: 총 건수, tier별 분포
3. pattern_ethics_map 테이블: 총 건수
4. 무작위 3개 패턴에 대해 get_ethics_for_patterns() 호출 결과
5. parent_code_id가 올바르게 매핑된 건수

결과를 보고해줘.
```

---

### STEP 17. Gamnamu — Supabase Studio 확인 및 클라우드 배포

**[시점]** STEP 16 완료 후.

**[Gamnamu 직접 수행]**
1. `http://localhost:54323`에서 각 테이블의 데이터 눈으로 확인
2. ethics_codes에서 JCE 접두어, is_citable 값 스팟 체크
3. 이상 없으면 배포 승인

**[프롬프트 — Claude Code CLI에게]**

```
M2 배포를 승인한다. 클라우드에 배포하라.

1. supabase db push 실행
2. 배포 후 원격 DB에서 검증:
   - SELECT count(*) FROM ethics_codes;
   - SELECT count(*) FROM patterns;
   - SELECT count(*) FROM pattern_ethics_map;
3. 결과 보고
```

---

### STEP 18. (Day 2 마무리) 세션 컨텍스트 갱신

**[프롬프트 — Claude Code CLI에게]**

```
M2가 완료되었다. 세션 컨텍스트를 갱신하라.

1. SESSION_CONTEXT 최신 버전을 업데이트:
   - M2 완료 상태 기록 (날짜, 각 테이블 레코드 수, 2-패스 결과)
   - is_citable = FALSE 확정 목록
   - relation_type 매핑 규칙 확정본
   - 감리 과정에서 발견/수정된 사항 요약
   - "다음 세션에서 할 일"을 M3(임베딩 생성 + 벤치마크)로 갱신
2. 이전 버전은 _archive_superseded/로 이동
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 비상 시나리오별 대응 프롬프트
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

### 시나리오 A: Claude Code CLI 컨텍스트 소진

**[증상]** 응답이 느려지거나 이전 지시를 잊는 경우

**[프롬프트 — Claude Code CLI에게]**

```
/compact

(compact 후)
현재 M[1/2] 작업의 [구체적 단계]를 진행 중이었다.
SESSION_CONTEXT 최신 버전과 CLAUDE.md를 다시 읽고,
마지막으로 완료한 작업과 다음 할 일을 보고하라.
```

### 시나리오 B: Migration 적용 실패

**[프롬프트 — Claude Code CLI에게]**

```
Migration 적용 중 오류가 발생했다.

■ 오류 메시지:
[오류 전문 붙여넣기]

1. 오류 원인을 분석하라
2. Migration 파일을 수정하라 (기존 파일 수정, 새 파일 생성 아님)
3. supabase db reset으로 처음부터 다시 적용
4. 수정 내용과 결과를 보고하라
```

### 시나리오 C: 두 감리 결과가 상충하는 경우

**[프롬프트 — Claude.ai에게]**

```
M[1/2] 감리에서 Claude.ai와 Antigravity의 판정이 상충한다.

■ Claude.ai 판정: [요약]
■ Antigravity 판정: [요약]
■ 상충 항목: [구체적으로]

양측 근거를 비교 분석하고, 어느 쪽이 타당한지 판단해줘.
마스터 플랜 v4.0을 기준으로 근거를 제시하고,
내가 최종 결정할 수 있도록 선택지와 각각의 트레이드오프를 설명해줘.
```

### 시나리오 D: Antigravity 레이트 리밋 발생

**[대응]** 
- Antigravity 2차 감리를 다음 세션으로 연기
- Claude.ai 감리만으로 진행하되, 감리 체크리스트를 더 꼼꼼히 수행
- SESSION_CONTEXT에 "2차 감리 미완료" 기록

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 타임라인 요약
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```
Day 1 (M1: 스키마)
├─ STEP 1:  CC CLI — 세션 시작 + 스키마 생성 지시
├─ STEP 2:  CC CLI — 파일 생성 완료
├─ STEP 3:  Claude.ai — 1차 감리
├─ STEP 4:  Antigravity — 2차 감리
├─ STEP 5:  Gamnamu — 비교·판단·(수정)
├─ STEP 6:  CC CLI — 로컬 DB 테스트
├─ STEP 7:  Antigravity MCP — 테이블 확인
├─ STEP 8:  Gamnamu — Studio 시각 확인 + 배포 승인
├─ STEP 9:  CC CLI — 클라우드 배포
└─ STEP 10: CC CLI — SESSION_CONTEXT 갱신

Day 2 (M2: 시드 데이터)
├─ STEP 11: CC CLI — 시드 스크립트 생성
├─ STEP 12: Claude.ai — 1차 감리
├─ STEP 13: Antigravity — 2차 감리 + MCP 확인
├─ STEP 14: Gamnamu — tier 분류 확인·판단·(수정)
├─ STEP 15: CC CLI — 로컬 적용 + 함수 테스트
├─ STEP 16: Antigravity MCP — 최종 확인
├─ STEP 17: Gamnamu — Studio 확인 + 클라우드 배포
└─ STEP 18: CC CLI — SESSION_CONTEXT 갱신

Day 3 (M3 이후 — 별도 플레이북 필요)
└─ 임베딩 생성 + 벤치마크 (기사 청킹 로직 프로토타이핑 포함)
```

---

*이 플레이북은 2026-03-28에 작성되었으며, 실제 작업 중 상황에 따라 유연하게 조정할 수 있다.*
